name: jobsync
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter

  ## == Local Storage == ##
  get_storage: ^2.1.1

  ## == State Management == ##
  get: ^4.7.2

  ## == Font == ##
  google_fonts: ^6.3.2

  ## == Animation == ##
  flutter_staggered_animations: ^1.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true